 /*let 
function multiply (num1, num2) {
    num1.isInteger();
    num2.isInteger();
    return num1 * num2;
}
function random (Number){
  return Number.isInteger(random);
}*/

const num1 = 4;
const num2 = 15.5;
const multiply = num1 * num2;

const random = Math.floor(Math.random() * 1000) + 1;

const num3 = num1;
const num4 = num2;
const mod = num1 % num2;

const max = Math.max(num1, num2, 20);